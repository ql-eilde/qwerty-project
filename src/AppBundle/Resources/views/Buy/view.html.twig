{% extends "AppBundle::layout.html.twig" %}

{% block title %}{{ product.name }} - {{ parent() }}{% endblock %}

{% block app_body %}
    <h1>Product Page</h1>
    <h2>{{ product.name }}</h2>
    <p>{{ product.description }}</p>
    <p>{{ product.price }} €</p>
    <p><a href="{{ path('cart_add', {'id': product.id}) }}" class="btn btn-primary">Add to cart</a></p>
{% endblock %}