{% extends 'AppBundle::layout.html.twig' %}

{% block title %}Recap - {{ parent() }}{% endblock %}

{% block app_body %}
    <h1>Product Information</h1>
    <p>{{ product.name }}</p>
    <p>{{ product.description }}</p>
    <p>{{ product.price }} €</p>
    
    <h2>Seller information</h2>
    <p>{{ user.email }}</p>
    <p>{{ user.iban.iban }}</p>

    <a href="{{ path('sell_validate') }}" class="btn btn-success">Valider</a>
{% endblock %}